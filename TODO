remove ionic entirely
implement view transitions using @capgo/capacitor-transitions (do this AFTER removing ionic entirely)
voice actor request is very slow: it's because there is a tmdb outage
add votes to trust work
add "untrusted" work
add gamification
add skeleton loaders
suivre un doubleur (notifications)
how do i push notifications
standardize back button or find an alternative
navigate routes by name , not by path