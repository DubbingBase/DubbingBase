Rechargement des le lancement de création de projet de doublage 
Nouveau projet: mauvais id. On sais pas si tv ou film
Plus de pull to refresh 
+ new buttons should be inside the pickers when no match found instead of inline in the view
use a picker instead of a select for job picking
implement view transitions
voice actor request is very slow: it's because there is a tmdb outage
add votes to trust work
add "untrusted" work
add gamification
add skeleton loaders
how do i push notifications
suivre un doubleur (notifications)
fix all any
replace native selects with mobile selects